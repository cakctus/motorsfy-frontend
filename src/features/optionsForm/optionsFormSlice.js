import { createSlice } from "@reduxjs/toolkit"

const initialState = []

export const optionsFormSlice = {
  name: "optionsForm",
  initialState,
  reducers: {},
}

export default optionsFormSlice.reducer
